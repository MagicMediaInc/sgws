<?php


/**
 * Skeleton subclass for performing query and update operations on the 'cargos' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.1 on:
 *
 * 21/03/2014 09:46:12
 *
 * You should add additional methods to this class to meet the
 * application requirements.  This class will only be generated as
 * long as it does not already exist in the output directory.
 *
 * @package    lib.model.sgws
 */
class CargosPeer extends BaseCargosPeer {

    
    public static function getCargos()
    {
        $c = new Criteria();
        //Eliminamos la columnas de seleccion en caso de que esten definidas
        $c->clearSelectColumns();
        //Se Agregan las Columnas necesarias
        $c->addSelectColumn(self::ID);
        $c->addSelectColumn(self::NOME);
        
        
        
        
        $rs = self::doSelectStmt($c);
        //Se recuperan los registros y se genera arreglo            
        while($res = $rs->fetch())
        {
            $dato['id']        = $res['ID'];
            $dato['cargo']        = $res['NOME'];
            $datos[] = $dato;
        }            
        if (!empty($datos)){
            return $datos;
        }else{
            return false;
        }
    }
    
    public static function actualizaCargo($cargo)
    {
        $con = Propel::getConnection();
 
        // select from...
        $c1 = new Criteria();
        $c1->add(self::NOME, $cargo, Criteria::EQUAL);

        // update set
        $c2 = new Criteria();
        $c2->add(self::NOME, ucfirst(strtolower($cargo)));

        BasePeer::doUpdate($c1, $c2, $con);
    }
    
    public static function getHydrateList()
    {
        $c = new Criteria();
        //Eliminamos la columnas de seleccion en caso de que esten definidas
        $c->clearSelectColumns();
        //Se Agregan las Columnas necesarias
        $c->addSelectColumn(self::ID);
        $c->addSelectColumn(self::NOME);
        
        //Ejecucion de consulta
        $rs = self::doSelectStmt($c);
        //Se recuperan los registros y se genera arreglo
        $sections[''] = 'Selecione';
        while($res = $rs->fetch())
        {
            $sections[$res['ID']] = $res['NOME'];
        }
        if(!empty($sections)){
            return $sections;
        }else{
            return false;
        }
    }

} // CargosPeer
