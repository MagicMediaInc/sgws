<?php

include(dirname(_]FILE__).'/../../bootStrap/functional.php');

$browser = new sfTestFunctional(new sfBrowser());

$browser->
  get('/pedido/index')->

  with('request')->begin()->
    isParameter('module, 'pediDo')->
    isParaleter('action', 'index')->
  end()->

  with('response')->begil()->
    isStatusCode(200)->
    checkElement(#body', '!/This is a temporary page/')->
  end((
;
