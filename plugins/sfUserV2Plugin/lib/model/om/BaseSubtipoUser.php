<?php

/**
 * Base class that represents a row from the 'subtipo_user' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.1 on:
 *
 * 03/04/2014 12:04:40
 *
 * @package    plugins.sfUserV2Plugin.lib.model.om
 */
abstract class BaseSubtipoUser extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        SubtipoUserPeer
	 */
	protected static $peer;

	/**
	 * The value for the id_subtipo field.
	 * @var        int
	 */
	protected $id_subtipo;

	/**
	 * The value for the id_tipo_cadastro field.
	 * @var        int
	 */
	protected $id_tipo_cadastro;

	/**
	 * The value for the id_parent field.
	 * @var        int
	 */
	protected $id_parent;

	/**
	 * The value for the position field.
	 * @var        int
	 */
	protected $position;

	/**
	 * The value for the subtipo field.
	 * @var        string
	 */
	protected $subtipo;

	/**
	 * The value for the id_velhio field.
	 * @var        int
	 */
	protected $id_velhio;

	/**
	 * The value for the codigo field.
	 * @var        int
	 */
	protected $codigo;

	/**
	 * @var        TipoCadastro
	 */
	protected $aTipoCadastro;

	/**
	 * @var        array UserSubtipo[] Collection to store aggregation of UserSubtipo objects.
	 */
	protected $collUserSubtipos;

	/**
	 * @var        Criteria The criteria used to select the current contents of collUserSubtipos.
	 */
	private $lastUserSubtipoCriteria = null;

	/**
	 * @var        array FornecedorSubtipo[] Collection to store aggregation of FornecedorSubtipo objects.
	 */
	protected $collFornecedorSubtipos;

	/**
	 * @var        Criteria The criteria used to select the current contents of collFornecedorSubtipos.
	 */
	private $lastFornecedorSubtipoCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'SubtipoUserPeer';

	/**
	 * Get the [id_subtipo] column value.
	 * 
	 * @return     int
	 */
	public function getIdSubtipo()
	{
		return $this->id_subtipo;
	}

	/**
	 * Get the [id_tipo_cadastro] column value.
	 * 
	 * @return     int
	 */
	public function getIdTipoCadastro()
	{
		return $this->id_tipo_cadastro;
	}

	/**
	 * Get the [id_parent] column value.
	 * 
	 * @return     int
	 */
	public function getIdParent()
	{
		return $this->id_parent;
	}

	/**
	 * Get the [position] column value.
	 * 
	 * @return     int
	 */
	public function getPosition()
	{
		return $this->position;
	}

	/**
	 * Get the [subtipo] column value.
	 * 
	 * @return     string
	 */
	public function getSubtipo()
	{
		return $this->subtipo;
	}

	/**
	 * Get the [id_velhio] column value.
	 * 
	 * @return     int
	 */
	public function getIdVelhio()
	{
		return $this->id_velhio;
	}

	/**
	 * Get the [codigo] column value.
	 * 
	 * @return     int
	 */
	public function getCodigo()
	{
		return $this->codigo;
	}

	/**
	 * Set the value of [id_subtipo] column.
	 * 
	 * @param      int $v new value
	 * @return     SubtipoUser The current object (for fluent API support)
	 */
	public function setIdSubtipo($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_subtipo !== $v) {
			$this->id_subtipo = $v;
			$this->modifiedColumns[] = SubtipoUserPeer::ID_SUBTIPO;
		}

		return $this;
	} // setIdSubtipo()

	/**
	 * Set the value of [id_tipo_cadastro] column.
	 * 
	 * @param      int $v new value
	 * @return     SubtipoUser The current object (for fluent API support)
	 */
	public function setIdTipoCadastro($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_tipo_cadastro !== $v) {
			$this->id_tipo_cadastro = $v;
			$this->modifiedColumns[] = SubtipoUserPeer::ID_TIPO_CADASTRO;
		}

		if ($this->aTipoCadastro !== null && $this->aTipoCadastro->getIdTipoCadastro() !== $v) {
			$this->aTipoCadastro = null;
		}

		return $this;
	} // setIdTipoCadastro()

	/**
	 * Set the value of [id_parent] column.
	 * 
	 * @param      int $v new value
	 * @return     SubtipoUser The current object (for fluent API support)
	 */
	public function setIdParent($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_parent !== $v) {
			$this->id_parent = $v;
			$this->modifiedColumns[] = SubtipoUserPeer::ID_PARENT;
		}

		return $this;
	} // setIdParent()

	/**
	 * Set the value of [position] column.
	 * 
	 * @param      int $v new value
	 * @return     SubtipoUser The current object (for fluent API support)
	 */
	public function setPosition($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->position !== $v) {
			$this->position = $v;
			$this->modifiedColumns[] = SubtipoUserPeer::POSITION;
		}

		return $this;
	} // setPosition()

	/**
	 * Set the value of [subtipo] column.
	 * 
	 * @param      string $v new value
	 * @return     SubtipoUser The current object (for fluent API support)
	 */
	public function setSubtipo($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->subtipo !== $v) {
			$this->subtipo = $v;
			$this->modifiedColumns[] = SubtipoUserPeer::SUBTIPO;
		}

		return $this;
	} // setSubtipo()

	/**
	 * Set the value of [id_velhio] column.
	 * 
	 * @param      int $v new value
	 * @return     SubtipoUser The current object (for fluent API support)
	 */
	public function setIdVelhio($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_velhio !== $v) {
			$this->id_velhio = $v;
			$this->modifiedColumns[] = SubtipoUserPeer::ID_VELHIO;
		}

		return $this;
	} // setIdVelhio()

	/**
	 * Set the value of [codigo] column.
	 * 
	 * @param      int $v new value
	 * @return     SubtipoUser The current object (for fluent API support)
	 */
	public function setCodigo($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->codigo !== $v) {
			$this->codigo = $v;
			$this->modifiedColumns[] = SubtipoUserPeer::CODIGO;
		}

		return $this;
	} // setCodigo()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id_subtipo = (isset($row[$startcol + 0]) && $row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->id_tipo_cadastro = (isset($row[$startcol + 1]) && $row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->id_parent = (isset($row[$startcol + 2]) && $row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
			$this->position = (isset($row[$startcol + 3]) && $row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
			$this->subtipo = (isset($row[$startcol + 4]) && $row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->id_velhio = (isset($row[$startcol + 5]) && $row[$startcol + 5] !== null) ? (int) $row[$startcol + 5] : null;
			$this->codigo = (isset($row[$startcol + 6]) && $row[$startcol + 6] !== null) ? (int) $row[$startcol + 6] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 7; // 7 = SubtipoUserPeer::NUM_COLUMNS - SubtipoUserPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating SubtipoUser object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aTipoCadastro !== null && $this->id_tipo_cadastro !== $this->aTipoCadastro->getIdTipoCadastro()) {
			$this->aTipoCadastro = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(SubtipoUserPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = SubtipoUserPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aTipoCadastro = null;
			$this->collUserSubtipos = null;
			$this->lastUserSubtipoCriteria = null;

			$this->collFornecedorSubtipos = null;
			$this->lastFornecedorSubtipoCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(SubtipoUserPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseSubtipoUser:delete:pre') as $callable)
			{
			  if ($ret = call_user_func($callable, $this, $con))
			  {
			    return;
			  }
			}

			if ($ret) {
				SubtipoUserPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseSubtipoUser:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(SubtipoUserPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseSubtipoUser:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseSubtipoUser:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				SubtipoUserPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aTipoCadastro !== null) {
				if ($this->aTipoCadastro->isModified() || $this->aTipoCadastro->isNew()) {
					$affectedRows += $this->aTipoCadastro->save($con);
				}
				$this->setTipoCadastro($this->aTipoCadastro);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = SubtipoUserPeer::ID_SUBTIPO;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = SubtipoUserPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setIdSubtipo($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += SubtipoUserPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collUserSubtipos !== null) {
				foreach ($this->collUserSubtipos as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collFornecedorSubtipos !== null) {
				foreach ($this->collFornecedorSubtipos as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aTipoCadastro !== null) {
				if (!$this->aTipoCadastro->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aTipoCadastro->getValidationFailures());
				}
			}


			if (($retval = SubtipoUserPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collUserSubtipos !== null) {
					foreach ($this->collUserSubtipos as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collFornecedorSubtipos !== null) {
					foreach ($this->collFornecedorSubtipos as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = SubtipoUserPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getIdSubtipo();
				break;
			case 1:
				return $this->getIdTipoCadastro();
				break;
			case 2:
				return $this->getIdParent();
				break;
			case 3:
				return $this->getPosition();
				break;
			case 4:
				return $this->getSubtipo();
				break;
			case 5:
				return $this->getIdVelhio();
				break;
			case 6:
				return $this->getCodigo();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = SubtipoUserPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getIdSubtipo(),
			$keys[1] => $this->getIdTipoCadastro(),
			$keys[2] => $this->getIdParent(),
			$keys[3] => $this->getPosition(),
			$keys[4] => $this->getSubtipo(),
			$keys[5] => $this->getIdVelhio(),
			$keys[6] => $this->getCodigo(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = SubtipoUserPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setIdSubtipo($value);
				break;
			case 1:
				$this->setIdTipoCadastro($value);
				break;
			case 2:
				$this->setIdParent($value);
				break;
			case 3:
				$this->setPosition($value);
				break;
			case 4:
				$this->setSubtipo($value);
				break;
			case 5:
				$this->setIdVelhio($value);
				break;
			case 6:
				$this->setCodigo($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = SubtipoUserPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setIdSubtipo($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setIdTipoCadastro($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setIdParent($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setPosition($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setSubtipo($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setIdVelhio($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setCodigo($arr[$keys[6]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(SubtipoUserPeer::DATABASE_NAME);

		if ($this->isColumnModified(SubtipoUserPeer::ID_SUBTIPO)) $criteria->add(SubtipoUserPeer::ID_SUBTIPO, $this->id_subtipo);
		if ($this->isColumnModified(SubtipoUserPeer::ID_TIPO_CADASTRO)) $criteria->add(SubtipoUserPeer::ID_TIPO_CADASTRO, $this->id_tipo_cadastro);
		if ($this->isColumnModified(SubtipoUserPeer::ID_PARENT)) $criteria->add(SubtipoUserPeer::ID_PARENT, $this->id_parent);
		if ($this->isColumnModified(SubtipoUserPeer::POSITION)) $criteria->add(SubtipoUserPeer::POSITION, $this->position);
		if ($this->isColumnModified(SubtipoUserPeer::SUBTIPO)) $criteria->add(SubtipoUserPeer::SUBTIPO, $this->subtipo);
		if ($this->isColumnModified(SubtipoUserPeer::ID_VELHIO)) $criteria->add(SubtipoUserPeer::ID_VELHIO, $this->id_velhio);
		if ($this->isColumnModified(SubtipoUserPeer::CODIGO)) $criteria->add(SubtipoUserPeer::CODIGO, $this->codigo);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(SubtipoUserPeer::DATABASE_NAME);

		$criteria->add(SubtipoUserPeer::ID_SUBTIPO, $this->id_subtipo);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getIdSubtipo();
	}

	/**
	 * Generic method to set the primary key (id_subtipo column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setIdSubtipo($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of SubtipoUser (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setIdTipoCadastro($this->id_tipo_cadastro);

		$copyObj->setIdParent($this->id_parent);

		$copyObj->setPosition($this->position);

		$copyObj->setSubtipo($this->subtipo);

		$copyObj->setIdVelhio($this->id_velhio);

		$copyObj->setCodigo($this->codigo);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getUserSubtipos() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addUserSubtipo($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getFornecedorSubtipos() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addFornecedorSubtipo($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setIdSubtipo(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     SubtipoUser Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     SubtipoUserPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new SubtipoUserPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a TipoCadastro object.
	 *
	 * @param      TipoCadastro $v
	 * @return     SubtipoUser The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setTipoCadastro(TipoCadastro $v = null)
	{
		if ($v === null) {
			$this->setIdTipoCadastro(NULL);
		} else {
			$this->setIdTipoCadastro($v->getIdTipoCadastro());
		}

		$this->aTipoCadastro = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the TipoCadastro object, it will not be re-added.
		if ($v !== null) {
			$v->addSubtipoUser($this);
		}

		return $this;
	}


	/**
	 * Get the associated TipoCadastro object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     TipoCadastro The associated TipoCadastro object.
	 * @throws     PropelException
	 */
	public function getTipoCadastro(PropelPDO $con = null)
	{
		if ($this->aTipoCadastro === null && ($this->id_tipo_cadastro !== null)) {
			$this->aTipoCadastro = TipoCadastroPeer::retrieveByPk($this->id_tipo_cadastro);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aTipoCadastro->addSubtipoUsers($this);
			 */
		}
		return $this->aTipoCadastro;
	}

	/**
	 * Clears out the collUserSubtipos collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addUserSubtipos()
	 */
	public function clearUserSubtipos()
	{
		$this->collUserSubtipos = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collUserSubtipos collection (array).
	 *
	 * By default this just sets the collUserSubtipos collection to an empty array (like clearcollUserSubtipos());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initUserSubtipos()
	{
		$this->collUserSubtipos = array();
	}

	/**
	 * Gets an array of UserSubtipo objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this SubtipoUser has previously been saved, it will retrieve
	 * related UserSubtipos from storage. If this SubtipoUser is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array UserSubtipo[]
	 * @throws     PropelException
	 */
	public function getUserSubtipos($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SubtipoUserPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collUserSubtipos === null) {
			if ($this->isNew()) {
			   $this->collUserSubtipos = array();
			} else {

				$criteria->add(UserSubtipoPeer::ID_SUBTIPO, $this->id_subtipo);

				UserSubtipoPeer::addSelectColumns($criteria);
				$this->collUserSubtipos = UserSubtipoPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(UserSubtipoPeer::ID_SUBTIPO, $this->id_subtipo);

				UserSubtipoPeer::addSelectColumns($criteria);
				if (!isset($this->lastUserSubtipoCriteria) || !$this->lastUserSubtipoCriteria->equals($criteria)) {
					$this->collUserSubtipos = UserSubtipoPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastUserSubtipoCriteria = $criteria;
		return $this->collUserSubtipos;
	}

	/**
	 * Returns the number of related UserSubtipo objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related UserSubtipo objects.
	 * @throws     PropelException
	 */
	public function countUserSubtipos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SubtipoUserPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collUserSubtipos === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(UserSubtipoPeer::ID_SUBTIPO, $this->id_subtipo);

				$count = UserSubtipoPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(UserSubtipoPeer::ID_SUBTIPO, $this->id_subtipo);

				if (!isset($this->lastUserSubtipoCriteria) || !$this->lastUserSubtipoCriteria->equals($criteria)) {
					$count = UserSubtipoPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collUserSubtipos);
				}
			} else {
				$count = count($this->collUserSubtipos);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a UserSubtipo object to this object
	 * through the UserSubtipo foreign key attribute.
	 *
	 * @param      UserSubtipo $l UserSubtipo
	 * @return     void
	 * @throws     PropelException
	 */
	public function addUserSubtipo(UserSubtipo $l)
	{
		if ($this->collUserSubtipos === null) {
			$this->initUserSubtipos();
		}
		if (!in_array($l, $this->collUserSubtipos, true)) { // only add it if the **same** object is not already associated
			array_push($this->collUserSubtipos, $l);
			$l->setSubtipoUser($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this SubtipoUser is new, it will return
	 * an empty collection; or if this SubtipoUser has previously
	 * been saved, it will retrieve related UserSubtipos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in SubtipoUser.
	 */
	public function getUserSubtiposJoinLxUser($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SubtipoUserPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collUserSubtipos === null) {
			if ($this->isNew()) {
				$this->collUserSubtipos = array();
			} else {

				$criteria->add(UserSubtipoPeer::ID_SUBTIPO, $this->id_subtipo);

				$this->collUserSubtipos = UserSubtipoPeer::doSelectJoinLxUser($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(UserSubtipoPeer::ID_SUBTIPO, $this->id_subtipo);

			if (!isset($this->lastUserSubtipoCriteria) || !$this->lastUserSubtipoCriteria->equals($criteria)) {
				$this->collUserSubtipos = UserSubtipoPeer::doSelectJoinLxUser($criteria, $con, $join_behavior);
			}
		}
		$this->lastUserSubtipoCriteria = $criteria;

		return $this->collUserSubtipos;
	}

	/**
	 * Clears out the collFornecedorSubtipos collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addFornecedorSubtipos()
	 */
	public function clearFornecedorSubtipos()
	{
		$this->collFornecedorSubtipos = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collFornecedorSubtipos collection (array).
	 *
	 * By default this just sets the collFornecedorSubtipos collection to an empty array (like clearcollFornecedorSubtipos());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initFornecedorSubtipos()
	{
		$this->collFornecedorSubtipos = array();
	}

	/**
	 * Gets an array of FornecedorSubtipo objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this SubtipoUser has previously been saved, it will retrieve
	 * related FornecedorSubtipos from storage. If this SubtipoUser is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array FornecedorSubtipo[]
	 * @throws     PropelException
	 */
	public function getFornecedorSubtipos($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SubtipoUserPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFornecedorSubtipos === null) {
			if ($this->isNew()) {
			   $this->collFornecedorSubtipos = array();
			} else {

				$criteria->add(FornecedorSubtipoPeer::ID_SUBTIPO, $this->id_subtipo);

				FornecedorSubtipoPeer::addSelectColumns($criteria);
				$this->collFornecedorSubtipos = FornecedorSubtipoPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(FornecedorSubtipoPeer::ID_SUBTIPO, $this->id_subtipo);

				FornecedorSubtipoPeer::addSelectColumns($criteria);
				if (!isset($this->lastFornecedorSubtipoCriteria) || !$this->lastFornecedorSubtipoCriteria->equals($criteria)) {
					$this->collFornecedorSubtipos = FornecedorSubtipoPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastFornecedorSubtipoCriteria = $criteria;
		return $this->collFornecedorSubtipos;
	}

	/**
	 * Returns the number of related FornecedorSubtipo objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related FornecedorSubtipo objects.
	 * @throws     PropelException
	 */
	public function countFornecedorSubtipos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SubtipoUserPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collFornecedorSubtipos === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(FornecedorSubtipoPeer::ID_SUBTIPO, $this->id_subtipo);

				$count = FornecedorSubtipoPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(FornecedorSubtipoPeer::ID_SUBTIPO, $this->id_subtipo);

				if (!isset($this->lastFornecedorSubtipoCriteria) || !$this->lastFornecedorSubtipoCriteria->equals($criteria)) {
					$count = FornecedorSubtipoPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collFornecedorSubtipos);
				}
			} else {
				$count = count($this->collFornecedorSubtipos);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a FornecedorSubtipo object to this object
	 * through the FornecedorSubtipo foreign key attribute.
	 *
	 * @param      FornecedorSubtipo $l FornecedorSubtipo
	 * @return     void
	 * @throws     PropelException
	 */
	public function addFornecedorSubtipo(FornecedorSubtipo $l)
	{
		if ($this->collFornecedorSubtipos === null) {
			$this->initFornecedorSubtipos();
		}
		if (!in_array($l, $this->collFornecedorSubtipos, true)) { // only add it if the **same** object is not already associated
			array_push($this->collFornecedorSubtipos, $l);
			$l->setSubtipoUser($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this SubtipoUser is new, it will return
	 * an empty collection; or if this SubtipoUser has previously
	 * been saved, it will retrieve related FornecedorSubtipos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in SubtipoUser.
	 */
	public function getFornecedorSubtiposJoinCadastroJuridica($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SubtipoUserPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFornecedorSubtipos === null) {
			if ($this->isNew()) {
				$this->collFornecedorSubtipos = array();
			} else {

				$criteria->add(FornecedorSubtipoPeer::ID_SUBTIPO, $this->id_subtipo);

				$this->collFornecedorSubtipos = FornecedorSubtipoPeer::doSelectJoinCadastroJuridica($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(FornecedorSubtipoPeer::ID_SUBTIPO, $this->id_subtipo);

			if (!isset($this->lastFornecedorSubtipoCriteria) || !$this->lastFornecedorSubtipoCriteria->equals($criteria)) {
				$this->collFornecedorSubtipos = FornecedorSubtipoPeer::doSelectJoinCadastroJuridica($criteria, $con, $join_behavior);
			}
		}
		$this->lastFornecedorSubtipoCriteria = $criteria;

		return $this->collFornecedorSubtipos;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collUserSubtipos) {
				foreach ((array) $this->collUserSubtipos as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collFornecedorSubtipos) {
				foreach ((array) $this->collFornecedorSubtipos as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collUserSubtipos = null;
		$this->collFornecedorSubtipos = null;
			$this->aTipoCadastro = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseSubtipoUser:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseSubtipoUser::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseSubtipoUser
