<?php


/**
 * Skeleton subclass for performing query and update operations on the 'info_banco_user' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.1 on:
 *
 * 02/02/2013 17:55:51
 *
 * You should add additional methods to this class to meet the
 * application requirements.  This class will only be generated as
 * long as it does not already exist in the output directory.
 *
 * @package    lib.model.sgws
 */
class InfoBancoUserPeer extends BaseInfoBancoUserPeer {
    
    public static function getListInfoBanco($id_user) {
        $c = new Criteria();
        //Eliminamos la columnas de seleccion en caso de que esten definidas
        $c->clearSelectColumns();
        //Selecciona las columnas
        $c->addSelectColumn(self::ID_INFO_BANCO);
        $c->addSelectColumn(self::ID_BANCO);
        $c->addSelectColumn(self::ID_USER);
        $c->addSelectColumn(self::TITULAR);
        $c->addSelectColumn(self::AGENCIA);
        $c->addSelectColumn(self::NUMERO_CONTA);
        
        $c->add(self::ID_USER, $id_user, Criteria::EQUAL);
        //Ejecucion de consulta
        $rs = self::doSelectStmt($c);
        //Primero se recuperan todos los modulos del Lynx
        while($res = $rs->fetch()) {
            $dato['id_info']        = $res['ID_INFO_BANCO'];
            $dato['id_banco']       = $res['ID_BANCO'];
            $dato['id_user']        = $res['ID_USER'];
            $dato['titular']        = $res['TITULAR'];
            $dato['agencia']        = $res['AGENCIA'];
            $dato['conta']          = $res['NUMERO_CONTA'];
            $datos[] = $dato;
        }
        if (!empty($datos)) {
            return $datos;
        }else {
            return false;
        }
    }

} // InfoBancoUserPeer
