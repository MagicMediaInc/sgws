<?php


/**
 * Skeleton subclass for performing query and update operations on the 'sf_seccion_archivos' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.1 on:
 *
 * 15/08/2012 04:12:54
 *
 * You should add additional methods to this class to meet the
 * application requirements.  This class will only be generated as
 * long as it does not already exist in the output directory.
 *
 * @package    plugins.sfLynxSectionPlugin.lib.model
 */
class SfSeccionArchivosPeer extends BaseSfSeccionArchivosPeer {
    
    public static function getFilesSection($idSection)
    {
        //Obtengo el idioma principal
        $c = new Criteria();
        //Eliminamos la columnas de seleccion en caso de que esten definidas
        $c->clearSelectColumns();
        //Se Agregan las Columnas necesarias        
        $c->addSelectColumn(self::ID_SECCION_ARCHIVO);
        $c->addSelectColumn(SfArchivosSeccionPeer::TITULO_ARCHIVO);
        $c->addSelectColumn(SfArchivosSeccionPeer::ARCHIVO);
        
        $c->addJoin(self::ID_ARCHIVO, SfArchivosSeccionPeer::ID_ARCHIVO_SECCION, Criteria::INNER_JOIN);
        //Filtros
        $c->add(self::ID_SECCION,$idSection, Criteria::EQUAL);
        $rs = self::doSelectStmt($c);
        //Se recuperan los registros y se genera arreglo
        while($res = $rs->fetch())
        {
            $dato['id_seccion_archivo'] = $res['ID_SECCION_ARCHIVO'];
            $dato['archivo'] = $res['TITULO_ARCHIVO'];
            $dato['file'] = $res['ARCHIVO'];
            $datos[] = $dato;
        }
        if (!empty($datos)){
            return $datos;
        }else{
            return false;
        }
    }
    
    public static function checkFileSeccion($id_section, $id_file) {

        $c = new Criteria();
        $c->add(self::ID_SECCION,$id_section, Criteria::EQUAL);
        $c->add(self::ID_ARCHIVO,$id_file, Criteria::EQUAL);
        return self::doCount($c);
        
    }
    

} // SfSeccionArchivosPeer
