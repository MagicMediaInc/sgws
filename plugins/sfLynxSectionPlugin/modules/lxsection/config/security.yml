all:
  is_secure: on


index:
  credentials: [lx_section_view]

new:
  credentials: [lx_section_view, lx_section_insert]

create:
  credentials: [lx_section_view, lx_section_insert]

edit:
  credentials: [lx_section_view, lx_section_update]

editContent:
  credentials: [lx_section_view, lx_section_update]

infoLanguage:
  credentials: [lx_section_view, lx_section_update]

treeSections:
  credentials: [lx_section_view]

listenerMove:
  credentials: [lx_section_update]

delete:
  credentials: [lx_section_view, lx_section_delete]

deleteAll:
  credentials: [lx_section_view, lx_section_delete]